<div class="container">
  <div class="row">
    <div class="col-md">
      <h3>Show Hotel Page</h3>

      <h3><%= @hotel.title %></h3>

      <p><%= @hotel.description %></p>

      <%= link_to "Edit Hotel Details", edit_hotels_path(@hotel) %>
      <br/>
      <%= link_to "Delete Hotel", hotels_path(@hotels), method: :delete, data: {confirm: "Are you sure?"} %>

    </div>
  </div>
</div>
